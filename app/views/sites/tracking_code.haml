.container
  %h2 Tracking Code
  %div 
    Last load at:
    - if @last_load
      = @last_load.time
    - else
      never
  %h3 Add this code immediately before your &lt;/body&gt; tag:
  #tracking_code><= display_tracking_code(0.1)
  %h2 Configuration
  %ul
    %li
      %b user_id 
      Your local identification (e.g. integer ID, slug, session hash) for the user
  %br
  = link_to 'Back', @site